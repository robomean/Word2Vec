on: push
name: on push
jobs:
  gitHubActionForPytest:
    name: Test Word2Vec
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@main
    - name: Test Word2Vec
      uses: robomean/Word2Vec@main
      with:
        args: python test.py
