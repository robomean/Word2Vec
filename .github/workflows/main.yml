on: push
name: on push
jobs:
  test-unittest:
    name: Test Word2Vec
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Test Word2Vec
        uses: robomean/Word2Vec@main
        with:
          args: python test.py
